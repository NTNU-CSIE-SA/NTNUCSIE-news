services:
  bot:
    build: .
    container_name: ntnucsie-news-bot
    restart: always # 確保程式崩潰或伺服器重啟時會自動恢復
    env_file: .env # 自動載入您的 TOKEN 等設定
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs